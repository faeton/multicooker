# Acceptance

Replace this with the minimum behavior a judge should be able to verify.

Use concrete checks:

- command to run;
- expected output shape;
- files that should be present;
- important edge case;
- what can remain stubbed.
