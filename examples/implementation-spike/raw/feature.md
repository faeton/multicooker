# Feature

Replace this with the narrow capability to prototype.

Good implementation-spike prompts have a visible end-to-end path, for
example:

- parse this config and resolve one route;
- import this data file and produce one report;
- run this state transition and show the resulting UI;
- expose this single command and verify its output.
