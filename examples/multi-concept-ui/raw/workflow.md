# Workflow

Replace this with the workflow the concepts must support.

A good workflow brief names:

- the user's starting state;
- the primary task;
- decisions the user must make;
- error or empty states;
- the successful end state.
